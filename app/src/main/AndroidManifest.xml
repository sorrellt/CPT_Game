<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.strobertchs.cptassignment">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SigninScreen"
            android:label="@string/title_activity_sign_in__screen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegisterScreen"
            android:label="@string/title_activity_register__screen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ListofTasks" />
        <activity android:name=".googleclassroom" />
        <activity android:name=".Mainschoolmap" />
        <activity android:name=".wednesday"/>
        <activity android:name=".memeoftheday" />
        <activity android:name=".contacts_btn"/>
        <activity android:name=".spiritvideo"></activity>
    </application>

</manifest>